{
  "name": "app-template",
  "version": "0.1.0",
  "private": true,
  "description": "Squelette d'app hub perso : expose GET /hub/summary conforme au contrat, prêt à forker.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "//vercel-build": "AU FORK, dès que l'app a une base : ajouter \"vercel-build\": \"npm run db:migrate && next build\". Vercel utilise ce script à la place de \"build\" quand il existe → les migrations s'appliquent à CHAQUE déploiement, prod et previews. Exigence de Marc : ne JAMAIS avoir à lancer une commande sur son PC. Idempotent (table de suivi Drizzle). Alternative vue chez JobAI : migrations appliquées au DÉMARRAGE de l'app (lib/migrations.ts, mémorisées par processus) — à préférer si l'app doit aussi se réparer seule hors déploiement.",
  "//hub-contract": "Épinglé sur un SHA et non un tag : le tag v1.1.0 n'a jamais pu être poussé (proxy git, 403 sur les refs de tag). Les 5 apps de l'écosystème épinglent le MÊME SHA = contenu v1.1.0 (bloc usage additif). Ne pas redescendre à #v1.0.0 : cette version ignore le bloc usage.",
  "dependencies": {
    "@mokarade/hub-contract": "github:MoKarade/hub-contract#2d37a61c337d485ab9ec716d9c7507972e807226",
    "next": "^15.5.22",
    "next-auth": "^5.0.0-beta.31",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.6",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.18.0",
    "eslint-config-next": "^15.5.4",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "overrides": {
    "postcss": "^8.5.24",
    "sharp": "^0.35.3"
  }
}
